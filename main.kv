#:import utils kivy.utils
#:import FirebaseLoginScreen firebaseloginscreen.firebaseloginscreen.FirebaseLoginScreen
WindowManager:
    FirstWindow:
    SecondWindow:


<FirstWindow>:
    name: "main"
    orientation:'vertical'
    id: firebase_login_screen
        name: "firebase_login_screen"
        debug: True
        remember_user: True
        require_email_verification: True
        web_api_key: "AIzaSyDoVmC0QLRjzHsPIHv71YJmolayn7LPcPc" # Found in Firebase -> Project Settings -> Web API Key
        on_login_success:
            # Defining this function lets you program what to do when the
            # user has logged in (probably you'll want to change screens)!
            # Get the important user info
            if self.login_state == 'in': app.user_localId = self.localId
            if self.login_state == 'in': app.user_idToken = self.idToken
            if self.login_state == "in": print("User logged in")

            if self.login_state == 'out': print("User logged out")
    # ---- Other screens in your app should be down here (below the FirebaseLoginScreen)


<SecondWindow>:
    name: "second"

    MDBoxLayout:
        pos_hint:{'x': 0, 'y': 0.8}
        orientation:'vertical'
        size_hint: 1, 0.2
        md_bg_color: 0, 1, 1, .5
        Label:
            text: "Добро пожаловать домой"
            color: 1, 1, 1, 1
        Label:
            text: "#User Name"
            color: 1, 1, 1, 1


    MDBoxLayout:
        pos_hint:{'x': 0, 'y': 0.7}
        orientation:'horizontal'
        size_hint: 1, 0.1
        md_bg_color: 1, 0, 1, .5
        Button:
            background_color: 0, 0, 0.2, 1
            text:"гостинная"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"спальня"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"кухня"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"ванная"
            on_release: app.root.current = "main"


    GridLayout:
        rows:2
        cols:2
        pos_hint:{'x': 0, 'y': 0.3}
        size_hint: 1, 0.4
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"



    MDBoxLayout:
        pos_hint:{'x': 0.025, 'y': 0.025}
        orientation:'horizontal'
        padding: 15
        spacing: 10
        size_hint: 0.950, 0.1
        md_bg_color: 0, 1, 1, .5
        radius: [16, ]
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"
        Button:
            background_color: 0, 0, 0.2, 1
            text:"next"
            on_release: app.root.current = "main"

